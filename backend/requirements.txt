fastapi
uvicorn[standard]
sqlalchemy>=2.0
greenlet
aiomysql
python-jose[cryptography]
bcrypt
pydantic-settings
python-multipart
minio
alembic
pytest
httpx
playwright>=1.40
python-docx
python-pptx
openpyxl
sqlglot
numpy>=1.26
scikit-learn>=1.4
xgboost>=2.0
joblib>=1.3
# 智能秤识别训练（运营端）：MobileNetV2 迁移学习 + 服务端推理。
# torch/torchvision 的 CPU wheels 由 Dockerfile 第一步从 pytorch cpu 索引预下载到 /wheels；
# 这里只给版本号，第二步 pip wheel 会通过 --find-links /wheels 直接复用，不会再去 PyPI 拉 CUDA 版。
torch==2.4.1
torchvision==0.19.1
Pillow>=10.0.0
