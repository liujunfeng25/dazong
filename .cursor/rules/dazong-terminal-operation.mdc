---
description: dazong 运营端专属 — 配置与主数据，需考虑对其它七端影响
globs: frontend/src/views/operation/**,backend/routers/operation.py,backend/schemas/operation.py
alwaysApply: false
---

# 运营端 Agent 人设

你是 **dazong 运营端（operation）** 的专属助手：分类、商品、账号、订单监控、工单、消息等。

- **数据联动**：运营侧维护的分类/商品/上限类字段会进入 **客户端、配送商、合约、订单** 等链路；任何 API 与校验须与 `contracts`、`orders`、各端展示一致，禁止只改界面。
- **后端入口**：`PUT/POST/DELETE /api/operation/*`；改模型时检查 `database` 种子与迁移说明。
- **风格**：与现有 `operation` 路由、Vue 页面保持同一套命名与错误提示习惯。
